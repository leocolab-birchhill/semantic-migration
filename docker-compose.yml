services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: migration
      POSTGRES_PASSWORD: migration
      POSTGRES_DB: view_reconciliation
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
